BEGIN
    EXECUTE IMMEDIATE 'DROP TABLE CCN_RESTRICTION';
EXCEPTION WHEN OTHERS THEN NULL;
END;
/

  CREATE TABLE CCN_RESTRICTION 
   (	RESTRICTION_ID NUMBER,
	OBJECT_NAME VARCHAR2(30),
	RESTRICTION_EFFECTIVE_DATE DATE,
	RESTRICTION_TERM_DATE DATE,
	SESSION_ID NUMBER,
	CONSTRAINT CCN_RESTRICTION_PK PRIMARY KEY (RESTRICTION_ID) ENABLE
   );
