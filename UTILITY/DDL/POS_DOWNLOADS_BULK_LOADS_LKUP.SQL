 -- created by rxv940 on 02/06/2018
 -- Run this in Utils schema
 
CREATE TABLE POS_DOWNLOADS_BULK_LOADS_LKUP
(
    BULK_LOADS_LKUP_ID      NUMBER         NOT NULL ENABLE, 
    MAINTENANCE_TYPE        VARCHAR2(50), 
    MAINTENANCE_DATE_START  DATE           DEFAULT SYSDATE, 
    FLAG                    VARCHAR2(1)    DEFAULT 'Y', 
    MAINTENANCE_DATE_END    DATE, 
     CONSTRAINT PK_BULK_LOADS_LKUP_ID PRIMARY KEY (BULK_LOADS_LKUP_ID)
);

 
