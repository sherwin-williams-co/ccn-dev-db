
  CREATE TABLE MAILING_DETAILS 
   (	MAIL_CATEGORY VARCHAR2(100 BYTE), 
	GROUP_ID VARCHAR2(3 BYTE), 
	SUBJECT VARCHAR2(1000 BYTE), 
	FROM_P VARCHAR2(1000 BYTE), 
	MESSAGE VARCHAR2(4000 BYTE), 
	SIGNATURE VARCHAR2(4000 BYTE), 
	 CONSTRAINT MAILING_DETAILS_FK1 FOREIGN KEY (GROUP_ID) REFERENCES CCN_UTILITY.MAILING_GROUP (GROUP_ID) ENABLE
   );
